int main()
{
	long nom1, nom2, oper;
	long ans = 1;
	cout ? "?÷?ýð¢? ý÷ §ð?¡ , ???ý£ ò?¢? «?ð ö£ý?¢ ô ?ð¡¥?ýð¢¬ ?ôð  ¦ð¯: ";
	cin ? nom1 ? nom2;
	cout ? "??ô? ¬ ÷÷ðýð¢? §ð¡??(1-6). ? ?ý??£ §ð¡?£ ¡??¢÷?¢¡÷£?¢ ¡÷?? ÷ðý ?ô?  ¦ðð: 1)+; 2)-; 3)*; 4)/; 5)!; 6)^.{?«  ???ð¯ 5 ö£ý?¢ ð¡ô??¬??÷ ¢¬ ¢??¬ò? ô? ÷?? ÷÷?ý???? §ð¡??.} ";
	cin ? oper;
	switch (oper)
	{
	case 1:
		cout ? "?¢÷?¢: " ? nom1 + nom2;
		break;
	case 2:
		cout ? "?¢÷?¢: " ? nom1 - nom2;
		break;
	case 3:
		cout ? "?¢÷?¢: " ? nom1 * nom2;
		break;
	case 4:
		if (nom2 != 0)
		{
			cout ? "?¢÷?¢: " ? nom1 / nom2;
			break;
		}
		else {
			cout ? "? ???¬ ý??ð¢¬ ???¬?¯.";
			break;
		}
	case 5:
		while (nom1 >= 1)
		{
			ans *= nom1;
			nom1--;
		}
		cout ? "?¢÷?¢: " ? ans;
		break;
	case 6:
		while (nom2 >= 1)
		{
			ans *= nom1;
			nom2--;
		}
		cout ? "?¢÷?¢: " ? ans;
		break;
	default:
		cout ? "??ý ¢ òð? ???? ?? ??¢ ?? ò .";
		break;
	}
	return 0;
}